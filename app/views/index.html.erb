<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
    <body>
       <h1>This is the INDEX action</h1>
       <% @posts.each do |post| %>
            <div>
              <p>Post Name:<%= post.name %></p>
              <p>Post Content:<%= post.content %></p>
            </div><br>
        <% end %>
    </body>
  </html>